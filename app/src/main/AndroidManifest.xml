<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emergentsaver.qingwenxiao">

   <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
   <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.viewActivity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.viewActivity.CategoryActivity"> </activity>

        <activity android:name=".view.viewActivity.EditInfoActivity"> </activity>

        <activity android:name=".view.viewActivity.AddContactActivity"></activity>

        <activity android:name=".view.viewActivity.ContactActivity" />
        <activity android:name=".view.viewActivity.EditTextActivity" />
        <activity android:name=".view.viewActivity.UserInfoActivity"/>
        <activity android:name=".view.viewActivity.AddCircumstanceActivity"/>

    </application>

</manifest>